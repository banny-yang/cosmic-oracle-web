# 开发环境（vite dev 自动加载）
# 指向部署服务器（与小程序 BASE_URL 一致），扫码登录票据才能与小程序确认请求落在同一后端；
# 该服务器 CORS 已放行 localhost:*。若需连本地后端联调（127.0.0.1:8080），
# 在 .env.development.local（不入库）里覆盖：VITE_API_BASE_URL=http://127.0.0.1:8080
VITE_API_BASE_URL=https://server.oracle.duimai.net

# dev 不加载统计脚本；站长平台验证码见 .env.production 注释
