# 开发环境（vite dev 自动加载）—— 连本地后端（Java 仓库 dev-watch 或 IDEA 启动，默认 8080）
# 仅发布构建（vite build 加载 .env.production）才连正式服务器。
# 如需临时连正式后端联调（如小程序扫码登录链路），在 .env.development.local（不入库）覆盖：
#   VITE_API_BASE_URL=https://server.oracle.duimai.net   # 该服务器 CORS 已放行 localhost:*
VITE_API_BASE_URL=http://127.0.0.1:8080

# dev 不加载统计脚本；站长平台验证码见 .env.production 注释
